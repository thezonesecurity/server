{
  "name": "pruebaproject",
  "version": "1.0.0",
  "description": "npm run clean = eliminar la carpeta dist npx ts -> node dist/index.js = para compilar sudo service mongod start = inicia servicio de mongoose",
  "main": "index.js",
  "dependencies": {
    "@types/express": "^4.17.12",
    "express": "^4.17.1",
    "express-handlebars": "^5.3.2",
    "mongoose": "^5.12.13",
    "morgan": "^1.10.0"
  },
  "scripts": {
    "build": "tsc",
    "dev": "nodemon ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/morgan": "^1.9.2"
  }
}
